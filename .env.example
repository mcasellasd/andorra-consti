# Generació de text (obligatòria per chatbot i interpretació IA)
GROQ_API_KEY=gsk_YOUR_GROQ_API_KEY_HERE

# Proveïdor LLM: groq (per defecte) o groq-local. salamandra = àlies de groq (compatibilitat)
# LLM_PROVIDER=groq

# RAG (cerca semàntica amb XLM-RoBERTa): desactivat per defecte per velocitat. Activa si vols context addicional.
# RAG_ENABLED=true

# Embeddings (opcional - per defecte s'utilitza XLM-RoBERTa local)
# OPENAI_API_KEY=sk_YOUR_OPENAI_API_KEY_HERE
# EMBEDDING_PROVIDER=xlm-roberta
